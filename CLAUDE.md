# このリポジトリでのClaudeの動き方

このリポジトリの持ち主はプログラマーではありません。常に日本語で、専門用語をかみくだいて話してください。コードの書き方よりも「何ができるようになるか」を中心に説明します。

## 最初にやること

作業を始める前に、次の2つを読んでください。

- `ai/profile.md` … 持ち主のプロフィール(やりたいこと・好み・状況)
- `ai/backlog.md` … これまでの提案の一覧と、持ち主の返答

## 基本方針: 車輪の再発明をしない

- 何かを実装するときは、**まず既存のOSS・ライブラリ・サービスを探す**(Web検索・GitHub)。ゼロから自作するのは、既存のものが見つからない・合わないときの最終手段。
- 選択肢が複数あるときは、「どれを使うか」を持ち主が選べるように、違いをかみくだいて示す。
- 持ち主の役割は「やりたいこと・自分の特性を伝えること」。実装の中身はClaudeと既存ツールの組み合わせで完結させる。

## プロフィールを育てる(自己進化の仕組み)

- 会話の中で持ち主の新しい情報(やりたいこと、好み、いまやっている活動、提案への感想)が出てきたら、`ai/profile.md` に追記して、作業のついでにコミットする。
- 提案への返事(気になる・採用・見送り)が出たら `ai/backlog.md` の状態を更新する。見送りの理由が分かったら、プロフィールの「提案の好み」に反映する。
- こうして貯まったファイルが、次の提案・次のセッションの精度を上げる。

## 「提案して」と言われたら

`/teian` スキル(`.claude/skills/teian/SKILL.md`)の手順に従う。

## 変更のルール

- `ai/` フォルダの中は、上記の目的のためにClaudeが自由に更新してよい。
- アプリ本体(`myapp/`、`intern/` など)は、持ち主の依頼があるときだけ変更する。
